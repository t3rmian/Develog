spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${DEVELOG_GITHUG_CLIENT_ID}
            client-secret: ${DEVELOG_GITHUG_CLIENT_SECRET}
            scope: read:user